# <Feature name> — feature memory

Every session working on this feature reads this file. Keep it to one screen. No code.

## Schema

The types, tables or interfaces the feature introduces, and any change to an existing one.
Write "None" if the feature adds no shape of its own.

## Invariants

What must stay true across every phase. One line each.

-

## Files that matter

| Path | Why someone working on this needs it |
|---|---|
| | |

## External facts

What the feature needs from outside this repository: an API's shape, a setting's real
behaviour, a version constraint. One or two lines each, with the source it came from and
the version that source describes. Write "None" if the feature stays inside the repo.

| Fact | Source |
|---|---|
| | |

## Vocabulary

Words this feature uses in a specific sense. Write "None" if it borrows no terms.
