# <One line naming the problem, not the fix>

- **Status**: open | closed
- **Severity**: crash | wrong behaviour | annoyance
- **Area**: <one of this repo's components>
- **Found**: <yyyy-mm-dd>
- **Triaged**: <yyyy-mm-dd> — now | next | someday | won't (omit until a triage pass has looked at it)

## What happens

The behaviour as observed. Include the exact message or the exact wrong value.

## How to reproduce

Numbered steps from a clean start. Say what the starting state is whenever it matters.

1.
2.

## What should happen instead

## Where it lives

The files or subsystems involved, as `path/to/file.ext:42`. Write "Unknown" if the
cause has not been found yet.

## Notes

Anything already ruled out, related issues as a link to their file, and any workaround
that gets someone past it in the meantime.

## Resolution

Filled in when the issue moves to `closed/`. What changed, and where to see it — a
commit, a pull request, or a plan.
